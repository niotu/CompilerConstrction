class IntegerRealMixed is
    method mixedCalculation(i : Integer, r : Real) : Real is
        var mixedSum : Real(i.Plus(r))
        var converted : Real(i.toReal())
        var backToInt : Integer(r.toInteger())
        var finalResult : Real(converted.Plus(Real(3.14)))
        return finalResult
    end
end

class Main is
    this() is
        var res : Real()
        var i : IntegerRealMixed()
        res := i.mixedCalculation(100,11.1)
        res.Print()
    end
end