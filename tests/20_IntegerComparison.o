class IntegerComparisons is
    method compare(x : Integer, y : Integer) : Boolean is
        var result : Boolean(false)
        if x.Less(y) then
            result := true
        end
        if x.Greater(Integer(0)) then
            result := result.And(true)
        end
        if x.LessEqual(y) then
            result := result.Or(false)
        end
        if x.GreaterEqual(Integer(5)) then
            result := result.Xor(true)
        end
        if x.Equal(y) then
            result := result.Not()
        end
        return result
    end
end

class Main is
    method main() is
        var res : Boolean()
        var i : IntegerComparisons()
        res := i.compare(100,20)
        res.Print()
    end
end