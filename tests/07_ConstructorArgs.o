class App is
    var x : Integer(0)
    var y : Integer(14)
    var z : Real(2.0)
    this(a : Integer) is
        x := a
    end
end

class Main is
    this() is
        var c : Integer(5)
        c.Print()
    end
end
