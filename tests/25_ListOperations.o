class ListOperations is
    method buildList() : Integer is
        var list : List[Integer](5)
        list := list.append(10)
        list := list.append(20)
        list := list.append(30)
        
        var first : Integer(list.head())
        var rest : List[Integer]()
        rest := list.tail()
        var second : Integer(rest.head())
        
        return first.Plus(second)
    end
end

class Main is
    this() is
        var res : Integer()
        var l : ListOperations()
        res := l.buildList()
        res.Print()
    end
end