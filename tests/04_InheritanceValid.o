class Animal is
    method Speak() : Integer is
        return 11111
    end

    method Walk() : Integer is
        return 101010
    end
end

class Dog extends Animal is
    method Speak() : Integer is
        return 22222
    end
end

class Main is
    method main() is
        var a : Animal()
        var d : Dog()
        Integer(a.Speak()).Print()
        Integer(d.Speak()).Print()
        Integer(a.Walk()).Print()
        Integer(d.Walk()).Print()
    end
end
