class Animal is
    method Speak() : Integer is
        return 11111
    end

    method Walk() : Integer is
        return 101010
    end
end

class Dog extends Animal is
    method Speak() : Integer is
        return 22222
    end
end

class Main is
    this() is
        var a : Animal()
        var d : Dog()
        a.Speak().Print()
        d.Speak().Print()
        a.Walk().Print()
        d.Walk().Print()
    end
end
