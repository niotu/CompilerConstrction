class Main is
    this() is
        var a : Array[Integer](5)
        var i : Integer(0)
        while i.Less(a.Length) loop
            a.set(i, i.Mult(i))
            i := i.Plus(1)
        end
        a.get(0).Print()
        a.get(1).Print()
        a.get(2).Print()
        a.get(3).Print()
        a.get(4).Print()
    end
end
