class ArraySquare is
    this() is
        var a : Array[Integer](5)
        var i : Integer(0)
        while i < a.Length loop
            a.set(i, i * i)
            i := i.Plus(1)
        end
    end
end
