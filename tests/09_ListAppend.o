class Main is
    this() is

    end

    method main() is
        var l : List[Integer]()
        l.append(1)
        l.append(2)
        l.append(3)
        var head : Integer(l.head()) // 1
        head.Print()
    end
end
