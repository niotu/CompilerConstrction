class Calc is
    var current : Real(0)

    method Add(a : Integer) : Real is
        current := current.Plus(a)
        return current
    end

    method Add(a : Real) : Real is
        current := current.Plus(a)
        return current
    end
    
    method getResult : Real is
        return current
    end

    this() is

    end
end

class Main is
    this() is
        var calc : Calc()
        var result : Real()
        result := calc.Add(5).Add(3).getResult()
    end
end