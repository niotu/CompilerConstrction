class Calc is
    var current : Real(0)

    method Add(a : Integer) is
        current := current.Plus(a)
    end

    method Add(a : Real) is
        current := current.Plus(a)
    end

    this() is

    end
end

class Main is
    this() is
        var calc : Calc()
        var result : Real()
        result := calc.Add(3)
    end
end