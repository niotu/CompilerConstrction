class Calc is
    var current : Integer(0)

    method Add(a : Integer) : Integer is
        current := current.Plus(a)
        return current
    end

    method Add(a : Real) : Real is
        current := current.Plus(a)
        return current
    end
    
    method getResult : Integer is
        return current
    end

    this() is

    end
end

class Main is
    this() is
        var calc : Calc()
        var result : Integer()
        result := calc.Add(5).Add(3).getResult()
    end
end