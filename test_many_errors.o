class Main is
    var arr : Array[Integer] = Array[Integer](5)
    
    method test() : Integer is
        var x
        var result : Real = Integer(10)
        return Boolean(true)
    end
    
    this() is
        var y
        this.arr.set(10, 999)
    end
end
